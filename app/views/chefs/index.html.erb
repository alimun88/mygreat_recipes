<div class="pannel">
 <%= render 'shared/page_title', title: "Showing all Chefs" %>
<div class="center">
  <%= will_paginate %>
</div>
  <% @chefs.each do |chef| %>
    <ul class="listing">
      <div class="row center">
        <div class="well col-md-6 col-md-offset-3">
          <li><%= link_to gravatar_for(chef, size: 120), chef_path(chef) %></li>
          <li class="chef-title"><%= link_to chef.chef_name, chef_path(chef) %></li>
          <li><small><%= pluralize(chef.recipes.count, "recipe") if chef.recipes %></small></li>
        </div>    
      </div>    
    </ul>
  <% end %>
  <div class="center">
    <%= will_paginate %>
  </div>
</div>